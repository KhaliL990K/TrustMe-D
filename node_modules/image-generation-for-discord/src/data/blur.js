const jimp = require('jimp')

async function blur(img) {
    if (!img) throw new Error(`Please provide a image.`)

    img = await jimp.read(img)
    img.blur(5)
    let raw;
    img.getBuffer(`image/png`, (err, buffer) => raw = buffer)
    return raw
}
module.exports = blur