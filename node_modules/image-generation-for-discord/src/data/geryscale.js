const jimp = require(`jimp`)

async function geryscale(img) {
    if (!img) throw new Error(`Please provide a image url.`)

    img = await jimp.read(img)
    img.greyscale()
    let raw;
    img.getBuffer(`image/png`, (err, buffer) => raw = buffer)
    return raw
}
module.exports = geryscale